<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <div class="row">
        <div class="col-12">
          <% if user_signed_in? %>

            <%= t('layout.layout_hello', username: current_user.username) %> | <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>

          <% else %>

            <%= link_to t('layout.layout_sign_in'), new_user_session_path %>

          <% end %>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
          <%= yield %>
        </div>
      </div>
    </div>
    
    

  </body>
</html>
